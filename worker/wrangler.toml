name = "gudanah-ats-ai-verdict"
main = "index.js"
compatibility_date = "2024-09-01"

[vars]
ALLOWED_ORIGIN = "*"
GEMINI_MODEL = "gemini-1.5-flash"

# GEMINI_API_KEY is a secret, not a var — set it with:
#   wrangler secret put GEMINI_API_KEY
# Never commit it here.
